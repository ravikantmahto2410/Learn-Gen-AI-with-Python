temperature = 40

while temperature < 100:
    temperature = temperature + 15
    print(f"Current temperature : {temperature}")
    
    # we can also write temperature = temperature + 15 as  temperature += 15

print("Tea is ready to Boil")